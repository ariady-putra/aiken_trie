{
  "name": "aiken-trie",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "aiken b && prettier -w ./plutus.ts",
    "build:debug": "aiken b -t verbose && prettier -w ./plutus.ts",
    "test": "bun test",
    "fmt": "pnpm exec prettier --write ."
  },
  "devDependencies": {
    "bun-types": "latest",
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0",
    "prettier": "^3.0.3"
  },
  "dependencies": {
    "@lucid-evolution/lucid": "^0.4.18",
    "blakejs": "^1.2.1",
    "prettier": "^3.1.1"
  }
}
